# ============================================================
# AutomatedFundraising — Environment Variables Template
# Copy this file to .env and fill in your values.
# NEVER commit .env to GitHub — it's in .gitignore.
# Store all values below as GitHub Actions Secrets as well.
# ============================================================

# ── Supabase ─────────────────────────────────────────────────
SUPABASE_URL=https://kjbbdqqqloljzxikblwa.supabase.co
# Use the modern publishable key (sb_publishable_...) — NOT the service role key.
# Access is governed by RLS policies; no admin-level permissions are exposed.
SUPABASE_PUBLISHABLE_KEY=sb_publishable_U-wrJ6Pu5DhmSDcon6JyAg_0cndO0ab

# ── SendGrid ─────────────────────────────────────────────────
SENDGRID_API_KEY=your-sendgrid-api-key-here

# ── Sender Identity ──────────────────────────────────────────
# Recommendation: warm, personal first name outperforms org names
SENDER_NAME=Hope from Furry Friends
SENDER_EMAIL=hope@furryfriendswa.org

# ── Campaign ─────────────────────────────────────────────────
FUNDRAISER_NAME=2026 Animal Rescue Campaign
UNSUBSCRIBE_BASE_URL=https://furryfriendswa.org/unsubscribe

# ── Optional: Google Search via SerpAPI ──────────────────────
# Get a free key at https://serpapi.com
# Leave blank to skip Google search (seed list still runs)
SERPAPI_KEY=

# Optional: Google Maps Platform (Places API / Geocoding API)
# Recommended for high-volume local discovery with coordinates via Places (New).
# Enable at least: Places API (New). Geocoding API is optional if you later switch
# origin geocoding from Nominatim to Google.
GOOGLE_MAPS_API_KEY=

# ── Optional: Playwright for JS-rendered pages ───────────────
# Set to true only if you need to scrape JavaScript-heavy sites
PLAYWRIGHT_ENABLED=false

# ── Batch Settings ───────────────────────────────────────────
BATCH_SIZE=50
SEND_DELAY_SECONDS=1.5
